# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# JSON file database (PII 포함 가능 — 절대 커밋 금지)
data/db/
data/backups/
backups/

# Uploaded assets (keep directory structure, ignore actual uploads)
/public/uploads/*
!/public/uploads/.gitkeep
!/public/uploads/products/
!/public/uploads/media/
!/public/uploads/reviews/
!/public/uploads/broadcasts/
!/public/uploads/settings/
!/public/uploads/misc/
/public/uploads/**/*
!/public/uploads/**/.gitkeep
# misc/ 는 큐레이션된 공개 자산(인증서·브랜드 이미지).
# 사용자 업로드가 아니라 저장소에 포함되어 모든 배포에 함께 나가야 한다.
!/public/uploads/misc/**

# try-n account credentials (per-project isolation, never commit)
.vercel-token
.backup-migration/

# 데이터 백업 — 로컬 보관용. 큰 바이너리/JSON 누적이라 git 에 안 올림.
backups/

# Drive 자산 로컬 캐시 (마이그레이션 중간 산물)
scripts/drive-cache/
.claude/
HANDOFF.md
docs/
new design/
scripts/
zoel-app.js
.env.vercel.production
scripts/factory-footage-cache/
scripts/production-video-thumbs/

# 빌드 파이프라인 부품은 scripts/ ignore 의 예외 — git 추적 필요
!scripts/copy-tinymce.mjs

# TinyMCE self-host 정적 자산 (prebuild 가 node_modules/tinymce 에서 복사)
/public/tinymce
